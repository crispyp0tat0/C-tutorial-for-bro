# C-tutorial-for-bro
This repo is not for you... this is for bro. If you're not bro, don't open this repo

# The main function

This function is needed for ever C program... without it, it won't run

There's 2 ways to do it:

```C
int main(void) {...}
```

or

```C
int main(int argc, char *argv[]) {...}
```